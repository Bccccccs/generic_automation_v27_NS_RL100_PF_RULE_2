# B3 G02：与 G01 完全相同的 pulse，仅将喷口改为 J06。
actuation:
  mode: pulse_singlejet
  n_jets: 24
  total_actuation_windows: 10
  actuation_window_duration: 0.1
  solver_time_step: 1.0e-4
  mass_flow_rate: 1.0
  jet_ids: [6]
  pulse_window_numbers: [5]

output:
  run_dir: runs/real_star/G02_J06_pulse/input
