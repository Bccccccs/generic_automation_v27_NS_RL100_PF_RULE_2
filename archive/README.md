# Archive

This directory is reserved for local legacy snapshots and migration references.

Large historical project copies under `archive/legacy/` are intentionally ignored
by Git. Keep only lightweight notes or explicit migration artifacts tracked.
